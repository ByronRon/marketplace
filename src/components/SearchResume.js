import React from "react";
import { styled } from "@mui/material/styles";

const Search = styled("div")({
  padding: "10px 10px",
});

const SearchResume = () => {
  return <Search>Explore templates for your business</Search>;
};

export default SearchResume;
